{
  "name": "sysack-functions",
  "description": "SYSACK Cloud Functions — Firebase",
  "scripts": {
    "serve":  "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs":   "firebase functions:log"
  },
  "engines": { "node": "22" },
  "main": "src/index.js",
  "dependencies": {
    "firebase-admin":     "^12.0.0",
    "firebase-functions": "^5.1.1",
    "nodemailer":         "^6.9.0",
    "node-fetch":         "^2.7.0"
  },
  "private": true
}
